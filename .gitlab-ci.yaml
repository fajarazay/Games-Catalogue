# To contribute improvements to CI/CD templates, please follow the Development guide at:
# https://docs.gitlab.com/ee/development/cicd/templates.html
# This specific template is located at:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Swift.gitlab-ci.yml

# Lifted from: https://about.gitlab.com/2016/03/10/setting-up-gitlab-ci-for-ios-projects/
# This file assumes an own GitLab CI runner, setup on a macOS system.
stages:
  - build
  - test
  - archive
  - deploy

build_project:
  stage: build
  script:
    - xcodebuild clean -project Games%20Catalogue.xcodeproj -scheme Games%20Catalogue | xcpretty
    - xcodebuild test -project Games%20Catalogue.xcodeproj -scheme Games%20Catalogue -destination 'platform=iOS Simulator,name=iPhone 12,OS=14.5' | xcpretty -s
  tags:
    - ios_14-5

archive_project:
  stage: archive
  script:
    - xcodebuild clean archive -archivePath build/Games%20Catalogue -scheme Games%20Catalogue
    - xcodebuild -exportArchive -exportFormat ipa -archivePath "build/Games%20Catalogue.xcarchive" -exportPath "build/Games%20Catalogue.ipa" -exportProvisioningProfile "ProvisioningProfileName"
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  artifacts:
    paths:
      - build/Games%20Catalogue.ipa
  tags:
    - ios_14-5
    
